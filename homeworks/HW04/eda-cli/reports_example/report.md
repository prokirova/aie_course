# EDA-отчёт

Исходный файл: `example.csv`

Строк: **76**, столбцов: **1**

## Параметры генерации отчёта

- Макс. гистограмм: `6`
- Top категорий: `5`
- Порог пропусков для проблемных колонок: `10.0%`

## Качество данных (эвристики)

- Оценка качества: **0.77**
- Макс. доля пропусков по колонке: **2.63%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

## Корреляция числовых признаков

Недостаточно числовых колонок для корреляции.

## Категориальные признаки

Показано top-5 значений для каждой категориальной колонки.
См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

Показано максимум 6 числовых колонок.
См. файлы `hist_*.png`.
