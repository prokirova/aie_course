# Отчёт по итоговому проекту по курсу «Инженерия Искусственного Интеллекта»

> Рекомендуемый объём отчёта: 3-5 страниц в эквиваленте Markdown/печатного текста.  
> Отчёт должен позволить преподавателю понять задачу, данные, выбранные модели и результаты экспериментов.

---

## 1. Паспорт проекта

- **Название проекта:** `Transformer Churn Intent API`
- **Автор:** `Прохорова Кира Юрьевна`
- **Группа:** `БФБО-01-24`
- **Контакт:** `@prokirova`
- **Ссылка на репозиторий:** `https://github.com/prokirova/aie_course`

Проект представляет end-to-end сервис для оценки риска оттока клиента по тексту обращения. В нём есть синтетический датасет на 100 000 строк, предобученный трансформер Qwen как encoder, классификатор поверх embeddings, FastAPI-сервис, Dockerfile, тесты и базовая наблюдаемость.


---

## 2. Постановка задачи и контекст


**Предметная область** - клиентская аналитика подписочного сервиса. Потенциальный пользователь - CRM-система или менеджер поддержки, которому нужно быстро понять, есть ли в обращении клиента намерение уйти.

**ML-задача** - бинарная классификация churn intent:

- вход: текст обращения `message` и метаданные клиента;
- выход: `churn_intent = 1`, если обращение похоже на намерение уйти, иначе `0`;
- API возвращает класс, вероятность и уровень риска `low` / `medium` / `high`.

**Основные метрики:** ROC-AUC, F1, precision, recall и accuracy. ROC-AUC показывает качество ранжирования клиентов по риску, F1 важен для баланса precision/recall.

---

## 3. Данные

Опишите, какие данные использует проект:

1. **Источник данных:**
   - Данные синтетические, генерируются в `src/data/generate.py` и сохранены в `data/churn_text_synthetic.csv`. В выборке 100 000 строк, положительный класс составляет `0.4216`.


2. **Структура данных:**
   - Признаки:

| Колонка | Тип | Смысл |
|---|---|---|
| `message` | text | текст обращения клиента |
| `urgency` | numeric | срочность обращения от 1 до 5 |
| `months_active` | numeric | сколько месяцев клиент активен |
| `support_tickets` | numeric | число обращений в поддержку |
| `discount_requested` | numeric/binary | просит ли клиент скидку |
| `channel` | categorical | канал обращения |
| `segment` | categorical | сегмент клиента |
| `churn_intent` | target | целевая переменная |

3. **Предобработка и EDA:**
   - EDA находится в `notebooks/01_eda.ipynb`. Основные наблюдения: данные не содержат персональных данных, классы умеренно несбалансированы, текстовые шаблоны намерения уйти хорошо отделимы от нейтральных/позитивных обращений.

---

## 4. Модели и подходы

**Финальный подход:**

1. `Qwen/Qwen2.5-0.5B-Instruct`, сохранённый локально в `models/qwen2.5-0.5b-instruct`, превращает `message` в embedding.
2. Числовые признаки проходят через `StandardScaler`.
3. Категориальные признаки проходят через `OneHotEncoder(handle_unknown="ignore")`.
4. Embedding текста и обработанные метаданные склеиваются в один вектор.
5. Поверх него обучается sklearn-классификатор.

Сравнивались:

- `DummyClassifier` как baseline;
- `LogisticRegression` поверх Qwen embeddings;
- `RandomForestClassifier` поверх Qwen embeddings.

---

## 5. Экспериментальный протокол и результаты

Здесь важно показать, что выбор финальной модели был осмысленным.

1. **Экспериментальный протокол:**
   - как вы делили выборку (train/val/test, k-fold и т.п.);
   - как считали метрики (на какой части данных, с какими параметрами).

2. **Сравнение моделей по метрикам:**

   Пример таблицы (адаптируйте под свои метрики):

   | Модель / конфигурация         | Описание                             | Метрика 1 | Метрика 2 | Комментарий              |
   |-------------------------------|--------------------------------------|-----------|-----------|--------------------------|
   | Baseline                      | LogisticRegression, стандартные фичи | 0.78      | 0.74      | Простая стартовая точка  |
   | Model A                       | CatBoost, базовые параметры          | 0.84      | 0.81      | Лучше по основным метрикам |
   | Model B                       | CatBoost, тюнинг гиперпараметров     | 0.86      | 0.83      | Финальная модель         |

3. **Выбор финальной модели:**
   - какая модель выбрана как финальная и почему (баланс точности, скорости, сложности);
   - какие trade-off’ы приходилось учитывать.
Полный датасет содержит 100 000 строк. Для ускорения воспроизводимого обучения embeddings считаются на sample `3000`, заданном в `configs/config.json`. Разбиение: stratified train/test, `test_size=0.2`, `random_state=42`.

| Модель | Accuracy | Precision | Recall | F1 | ROC-AUC | Комментарий |
|---|---:|---:|---:|---:|---:|---|
| `dummy_most_frequent` | 0.585 | 0.000 | 0.000 | 0.000 | 0.500 | формальный baseline |
| `transformer_embeddings_logistic_regression` | 1.000 | 1.000 | 1.000 | 1.000 | 1.000 | финальная модель |
| `transformer_embeddings_random_forest` | 1.000 | 1.000 | 1.000 | 1.000 | 1.000 | качество такое же, модель тяжелее |

Финальная модель - `transformer_embeddings_logistic_regression`. Она выбрана, потому что на синтетической задаче даёт максимальные метрики, при этом проще, быстрее и легче для API, чем RandomForest. Идеальные метрики объясняются синтетической природой данных: формулировки churn intent в генераторе достаточно явно отделены от позитивных/нейтральных обращений.

---

## 6. Архитектура решения и сервис

Опишите, как из модели получился работающий сервис:

1. **Архитектура пайплайна:**
   - как связаны между собой загрузка данных, предобработка, модель и сервис;
   - можно приложить простую схему (текстом или картинку).

2. **API и endpoints:**
   - какие endpoints реализованы (минимум: `/health`, `/predict`);
   - какие параметры принимает `/predict` и что возвращает.

3. **Технологический стек:**
   - какие библиотеки и фреймворки используются;
   - используется ли Docker, какие есть команды для поднятия сервиса.

---

## 7. Наблюдаемость, конфигурация и безопасность

Кратко опишите:

1. **Логи и наблюдаемость:**
   - что логируется (запросы, ошибки, важные события);
   - как можно понять, что сервис работает корректно.

2. **Конфигурации:**
   - какие параметры вынесены в конфиги (`configs/`, `.env.example`);
   - что можно перенастроить без изменения кода.

3. **Безопасность:**
   - как вы избегали коммита секретов (токенов, паролей);
   - нет ли в репозитории “чувствительных” данных.

---

## 8. Ограничения и дальнейшая работа

Будьте честны:

- какие у проекта есть ограничения (качество, скорость, устойчивость, объём данных и т.п.);
- что бы вы сделали, если бы было больше времени:
  - улучшения модели;
  - улучшения сервиса;
  - улучшения данных и экспериментов.

---

## 9. Сценарий демонстрации на защите

Опишите, как вы планируете демонстрировать проект:

1. Что вы **запускаете**:
   - сервис, ноутбук, скрипт и т.п.;
2. Какие **1-2 ключевых сценария** вы показываете:
   - пример запроса к `/predict`;
   - пример работы с реальными/демонстрационными данными;
3. На что хотите, чтобы преподаватель обратил внимание:
   - качество предсказаний;
   - устойчивость;
   - удобство API;
   - архитектуру.

Ссылайтесь на команды и инструкции из `project/README.md`.

---
